# ProxyAdmin - 代理服务销售管理后台

一个用于代理服务销售管理的后台演示面板，包含完整的订单管理、会员管理、数据统计和系统配置功能。

## 📁 项目结构
```
proxy-admin-panel/
├── index.html                    # 主入口文件
├── css/
│   └── style.css                 # 样式文件
├── js/
│   ├── app.js                    # 主应用入口
│   ├── services/
│   │   └── api.js                # API服务层（集中管理所有API调用）
│   ├── data/
│   │   ├── mock-data.js          # 模拟数据（可删除）
│   │   └── config.js             # 配置常量
│   ├── modules/
│   │   ├── navigation.js         # 导航与页面切换
│   │   ├── modal.js              # 弹窗控制
│   │   ├── dashboard.js          # 仪表盘功能
│   │   ├── orders.js             # 订单管理
│   │   ├── members.js            # 会员管理
│   │   └── settings.js           # 系统设置
│   ├── utils/
│   │   └── helpers.js            # 工具函数（含Excel导出）
│   └── charts/
│       └── charts.js             # ECharts图表
└── README.md                     # 项目说明
```

## 📋 功能模块

1. **仪表盘** - 数据概览与可视化分析
2. **订单总表** - 订单管理、查询与导出
3. **会员管理** - 用户账户管理与导出
4. **每日数据概览** - 日度经营数据统计与导出
5. **系统设置** - 套餐配置与管理员管理

## 🛠️ 技术栈

- HTML5 / CSS3 / JavaScript (ES6+)
- ECharts 5.4.3 - 数据可视化
- SheetJS 0.18.5 - Excel导出
- Font Awesome 6.4.0 - 图标库

## 🔌 API集成指南

### 快速切换到真实API

所有API调用集中在 `js/services/api.js` 文件中，只需修改以下配置即可切换到真实API：
```javascript
const ApiService = {
    // 1. 设置你的API基础地址
    API_BASE_URL: 'https://your-api-domain.com/v1',
    
    // 2. 关闭模拟数据模式
    USE_MOCK_DATA: false,
    
    // 3. 配置认证Token获取方式
    getAuthToken: function() {
        return localStorage.getItem('auth_token');
    },
    
    // ...
};
```

### API端点列表

| 模块 | 方法 | 端点 | 说明 |
|------|------|------|------|
| **仪表盘** | GET | `/dashboard/stats` | 获取统计数据 |
| | GET | `/dashboard/trend` | 获取趋势图数据 |
| | GET | `/dashboard/sales-pie` | 获取销售额饼图数据 |
| | GET | `/dashboard/orders-pie` | 获取订单量饼图数据 |
| | GET | `/wallet/balance` | 获取钱包余额 |
| **订单** | GET | `/orders` | 获取订单列表 |
| | POST | `/orders` | 创建订单 |
| | GET | `/orders/export` | 导出订单数据 |
| **会员** | GET | `/members` | 获取会员列表 |
| | PUT | `/members/:username/password` | 修改会员密码 |
| | POST | `/members/:username/deduct` | 扣除会员资源 |
| | PUT | `/members/:username/status` | 切换会员状态 |
| | GET | `/members/:username/orders` | 获取会员订单历史 |
| | GET | `/members/resource` | 查询用户资源余额 |
| | GET | `/members/export` | 导出会员数据 |
| **每日数据** | GET | `/daily` | 获取每日数据列表 |
| | GET | `/daily/export` | 导出每日数据 |
| **套餐配置** | GET | `/packages/residential` | 获取住宅代理套餐 |
| | POST | `/packages/residential` | 创建住宅代理套餐 |
| | PUT | `/packages/residential/:id` | 更新住宅代理套餐 |
| | DELETE | `/packages/residential/:id` | 删除住宅代理套餐 |
| | PUT | `/packages/residential/:id/status` | 切换套餐状态 |
| | GET | `/packages/unlimited` | 获取无限量代理套餐 |
| | POST | `/packages/unlimited` | 创建无限量代理套餐 |
| | PUT | `/packages/unlimited/:id` | 更新无限量代理套餐 |
| | DELETE | `/packages/unlimited/:id` | 删除无限量代理套餐 |
| | PUT | `/packages/unlimited/:id/status` | 切换套餐状态 |
| **管理员** | GET | `/admins` | 获取管理员列表 |
| | POST | `/admins` | 创建管理员 |
| | PUT | `/admins/:id` | 更新管理员 |
| | DELETE | `/admins/:id` | 删除管理员 |
| **认证** | POST | `/auth/login` | 登录 |
| | POST | `/auth/logout` | 退出登录 |

### API响应格式建议
```javascript
// 列表数据响应格式
{
    "list": [...],      // 数据列表
    "total": 100,       // 总条数
    "page": 1,          // 当前页
    "pageSize": 20      // 每页条数
}

// 单条数据响应格式
{
    "id": 1,
    "name": "...",
    // ...其他字段
}

// 操作响应格式
{
    "success": true,
    "message": "操作成功"
}
```

## 📊 数据导出功能

系统支持将以下数据导出为Excel文件（.xlsx格式）：

- **订单总表** - 包含所有订单信息
- **会员管理** - 包含会员基本信息和余额
- **每日数据概览** - 包含每日经营数据

导出功能会自动应用当前的筛选条件。

## 📝 模拟数据说明

所有模拟数据集中在 `js/data/mock-data.js` 文件中，包括：

| 数据项 | 说明 |
|--------|------|
| `MockData.cardData` | 仪表盘数据卡片 |
| `MockData.orders` | 订单列表 |
| `MockData.members` | 会员列表 |
| `MockData.userOrders` | 用户订单历史 |
| `MockData.userResources` | 用户资源余额 |
| `MockData.dailyData` | 每日数据统计 |
| `MockData.residentialPackages` | 动态住宅代理套餐配置 |
| `MockData.unlimitedPackages` | 无限量代理套餐配置 |
| `MockData.admins` | 管理员账户 |
| `ChartMockData` | 图表数据 |

**注意**：当 `ApiService.USE_MOCK_DATA` 设置为 `false` 后，可以删除 `mock-data.js` 文件。

## 🚀 快速开始

### 开发模式（使用模拟数据）

1. 下载所有文件并保持目录结构
2. 使用浏览器打开 `index.html`
3. 或使用本地服务器运行（推荐）
```bash
# 使用 Python 启动本地服务器
python -m http.server 8080

# 或使用 Node.js
npx serve .
```

### 生产模式（对接真实API）

1. 修改 `js/services/api.js` 中的配置：
```javascript
   API_BASE_URL: 'https://your-api-domain.com/v1',
   USE_MOCK_DATA: false,
```

2. 根据需要配置认证Token获取方式

3. 可选：删除 `js/data/mock-data.js` 文件并移除 `index.html` 中的引用

4. 部署到Web服务器

## ⚠️ 注意事项

- 默认为**演示模式**，使用模拟数据
- 切换到生产模式前，请确保后端API已正确实现
- 建议使用 Chrome、Firefox、Edge 等现代浏览器访问
- 最佳显示分辨率：1920×1080 或更高
- Excel导出功能依赖 SheetJS 库（已通过CDN引入）

## 🔧 自定义配置

### 修改导出文件名格式

在 `js/utils/helpers.js` 中修改 `exportData` 方法：
```javascript
const fileName = typeName + '_' + this.getToday() + '.xlsx';
```

### 修改请求超时时间

在 `js/services/api.js` 中修改：
```javascript
TIMEOUT: 30000, // 毫秒
```

### 添加新的API端点

在 `js/services/api.js` 中添加新方法，参考现有方法格式：
```javascript
newApiMethod: async function(params) {
    if (this.USE_MOCK_DATA) {
        // 返回模拟数据
        return { success: true, data: mockData };
    }
    
    // 调用真实API
    return await this.request('/your-endpoint', {
        method: 'POST',
        body: params
    });
},
```

## 📋 后续开发建议

如需进一步开发，建议：

1. ✅ 接入后端API实现数据持久化（已预留接口）
2. ✅ 添加数据导出功能（已实现Excel导出）
3. 添加用户认证与权限管理
4. 集成真实的日期选择器组件
5. 实现表格排序与高级筛选
6. 添加数据缓存机制提升性能
7. 引入状态管理（如Vue/React）提升可维护性
8. 添加单元测试和集成测试

## 📄 许可证

MIT License

---

**ProxyAdmin** © 2025